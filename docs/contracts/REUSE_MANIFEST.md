# Clean-room reuse manifest

This manifest is the review record for evidence from
`/Users/zzhang/Documents/Shiba` at commit
`6af593c3f30a4519592c53f0abca96ff74a89e66` (the evidence snapshot is 2026-08-02).
Legacy gate scripts use their bytes at that commit; the common harness is
`/Users/zzhang/Documents/Shiba/scripts/lib/v2-pg-gate.sh` (banner
`shiba-v2-pg-gate-source-v1`). PG17 is 17.10 and PG18 is 18.4 in the recorded
environment. Only the Protocol JSON/schema and canonical digest rows below were
migrated, as data-only fixtures. Every legacy path and legacy PG scenario in
this table remains provenance only; M2--M11 re-proved the rows explicitly
marked below using independently written clean-room code and tests. A script
named in a PG17/18 evidence cell means both exact commands were run:
`PG_CONFIG=/opt/homebrew/opt/postgresql@17/bin/pg_config ./scripts/<script>` and
`PG_CONFIG=/opt/homebrew/opt/postgresql@18/bin/pg_config ./scripts/<script>`.
An unmarked legacy scenario is not evidence of clean-room behavior.

| 成果 | 来源 | 分类A/B/C | 复用方式 | 证据 | 未证明边界 |
|---|---|---|---|---|---|
| Protocol JSON/schema 样例 | `/Users/zzhang/Documents/Shiba/crates/shiba-protocol/src/lib/tests.rs`; `/Users/zzhang/Documents/Shiba/crates/shiba-protocol/src/primitive.rs` | A | 已迁移为纯数据 fixture `tests/fixtures/protocol/canonical-v1.json`；不复制旧实现或文档段落 | `canonical-v1.provenance.md`；PG17/18 `scripts/test-l0.sh` 联合 Protocol 定向测试在 clean-room 重证明固定 JSON shape、严格解码与 round-trip；M10 已另行证明 production transport，但不把 legacy fixture 当 transport 证据 | canonical-v1 的跨语言实现仍未证明 |
| canonical digest 测试向量 | `/Users/zzhang/Documents/Shiba/crates/shiba-protocol`; `/Users/zzhang/Documents/Shiba/scripts/test-contract-surface.sh` | A | 已迁移为同一纯数据 fixture；在新 `WireEnvelope`/domain-separated SHA-256 边界独立实现 | `canonical-v1.provenance.md`；L0 fixture 校验与 `canonical_wire_vector_is_stable_and_roundtrips` 共同重证明 canonical bytes、digest 与语义敏感性 | 非 Phase-1 message、跨语言实现未证明 |
| PG17/18 行为差分样例 | `/Users/zzhang/Documents/Shiba/docs/v2/TEST_MATRIX.md`; `/Users/zzhang/Documents/Shiba/scripts/test-v2-pg-gate-harness.sh` | A | `tests/fixtures/pg/deferred-evidence.json` 仅保存旧证据索引；M2--M12 的各行由 clean-room 脚本逐项重证明，不执行或移植旧 harness | PG17.10/PG18.4：本表 M2--M12.6 各里程碑行列出的 exact script 是逐项证据，均按表前两个 exact `PG_CONFIG` 命令执行；该 aggregate row 不把 legacy harness 计为证据 | 未列入后续里程碑行的 legacy scenario 仍未证明；cross-host soak、slot failover 与 reconnect supervisor 未证明 |
| pgoutput 解码边界样例 | `/Users/zzhang/Documents/Shiba/scripts/test-pgoutput-tuple-shapes-v2.sh` | A | 旧 frame expectation 只作 provenance；M3--M6 独立实现严格 tuple/transaction decoder，M10 在唯一 semantic decoder 外增加 bounded production transport/assembly | PG17.10/PG18.4 `scripts/test-m3.sh`、`scripts/test-m4.sh`、`scripts/test-m4-empty.sh`、`scripts/test-m4-update.sh`、`scripts/test-m4-delete.sh`、`scripts/test-m5-toast.sh`、`scripts/test-m5-incompressible-toast.sh`、`scripts/test-m5-composite-delete.sh`、`scripts/test-m5-replica-index.sh`、`scripts/test-m6-stream-commit.sh`、`scripts/test-m6-stream-abort.sh`、`scripts/test-m10-committed-ingress.sh`、`scripts/test-m10-streaming-ingress.sh` | 未支持的 pgoutput shape、interleaved XID/subtransaction、disconnect/TLS 与跨版本 future tag 仍 fail closed/未证明 |
| TOAST、NULL、空 tuple、streaming transaction 向量 | `/Users/zzhang/Documents/Shiba/scripts/test-pgoutput-toast-*-v2.sh`; `/Users/zzhang/Documents/Shiba/scripts/test-pgoutput-tuple-shapes-v2.sh`; `/Users/zzhang/Documents/Shiba/scripts/test-replication-ingress.sh` | A | 旧向量仅作 provenance；nullable int8/empty tuple 由 M4、TOAST text 由 M5、stream commit/abort 由 M6、production protocol-v2 transport/feedback 由 M10.3 独立重证明 | PG17.10/PG18.4 `scripts/test-m4.sh`、`scripts/test-m4-empty.sh`、`scripts/test-m5-toast.sh`、`scripts/test-m5-incompressible-toast.sh`、`scripts/test-m6-stream-commit.sh`、`scripts/test-m6-stream-abort.sh`、`scripts/test-m10-streaming-ingress.sh` | binary/NULL text、TOAST key、interleaved XID/subtransaction、跨主机 partial-stream/failover 未证明 |
| source identity / replica identity 规则 | `/Users/zzhang/Documents/Shiba/docs/v2/source-identity-registry.md`; `/Users/zzhang/Documents/Shiba/scripts/test-source-identity-*-v2.sh` | A | 旧规则仅作 provenance；M4.6/M5.4--M5.5 重证明 replica/source admission，M7 重证明 ObjectAddress DDL invalidation，M10.4 重证明 exact publication/slot/config/generation governance；M12 clean-room correction 以 retained lifecycle marker 区分 pre-M12 exact-3 与 M12-produced exact-4 binding，并把 primary-index OID 纳入同一 `source_binding` authority | PG17.10/PG18.4 M4--M10 exact gates；M12 identity 由 `scripts/test-m12-rebuild-identity-authority.sh` 证明；M12.3 `scripts/test-m12-rebuild-snapshot-live.sh` 双版本证明 exact-four authority 不经第二次切换完成 snapshot-to-live；M12.5 `scripts/test-m12-rebuild-governance.sh` 证明 target ObjectAddress/role governance；M12.6 release matrix 全绿 | same-OID rename仅窄 reconcile，replacement OID fail closed；FULL/NOTHING 与其他 shape 未证明 |
| catalog binding 字段语义 | `/Users/zzhang/Documents/Shiba/docs/v2/source-registry-apply-protocol.md`; `/Users/zzhang/Documents/Shiba/scripts/test-catalog-bindings-v2.sh` | A | 旧字段/SQL workflow 只作 provenance；M7 建立 exact ObjectAddress binding/invalidation，M9/M10/M11 建立 operator/config/bootstrap；M12 不增表，以持续 retained triple 标记 M12-produced generation，并将 exact identity-index ObjectAddress 写入既有 `source_binding`，禁止恢复时动态替换 | M7--M11 证据沿用各 clean-room exact scripts；M12 failure-first PG17 暴露无括号 `IF CASE`，随后 PG17.10/PG18.4 `scripts/test-m12-rebuild-identity-authority.sh`、`scripts/test-m12-rebuild-snapshot-live.sh`、`scripts/test-m12-rebuild-governance.sh` 与最终 release matrix 全绿 | M12 声明范围已证明；不存在第二 binding authority；更广 source/operator shape 未证明 |
| continuation/CAS 不变量 | `/Users/zzhang/Documents/Shiba/docs/v2/INVARIANTS.md`; `/Users/zzhang/Documents/Shiba/scripts/test-continuation-store-v2.sh` | A | 旧 store 需求只作 provenance；M2/M3 建立同事务 continuation/replay，M8 重证明同源 CAS/并发，M9 重证明多算子原子性，M10 区分 compute continuation 与 slot cursor，M11 使用独立 bootstrap checkpoint 且不伪造 WAL continuation，M12 证明 rebuild generation CAS | PG17.10/PG18.4 `scripts/test-m2.sh`、`scripts/test-m3.sh`、`scripts/test-m8-concurrent-sources.sh`、`scripts/test-m9-count-sum.sh`、`scripts/test-m9-operator-concurrency.sh`、`scripts/test-m10-committed-ingress.sh`、`scripts/test-m10-streaming-ingress.sh`、`scripts/test-m11-bootstrap.sh`、`scripts/test-m11-recovery.sh`、`scripts/test-m12-rebuild-admission.sh`、`scripts/test-m12-rebuild-recovery.sh` | slot loss/failover 与 automatic reconnect supervision 未证明 |
| DDL invalidation ObjectAddress 语义 | `/Users/zzhang/Documents/Shiba/scripts/test-ddl-invalidation-v2.sh`; `/Users/zzhang/Documents/Shiba/docs/v2/INVARIANTS.md` | A | relation/column/index rename、type、drop/cascade 与并发 Apply failure cases 已由 M7.1–M7.5 clean-room 重证；M12 进一步证明 active rebuild exact target ObjectAddress；不按名称替代 | PG17.10/PG18.4 `scripts/test-m7-ddl-invalidation.sh`、`scripts/test-m7-drop-invalidation.sh`、`scripts/test-m7-column-invalidation.sh`、`scripts/test-m7-index-invalidation.sh`、`scripts/test-m7-concurrent-ddl.sh`、`scripts/test-m12-rebuild-governance.sh`；旧绝对路径仅保留 provenance | privileged indistinguishable slot replacement 与更广 source shape 未证明 |
| crash/rollback/concurrency/performance 矩阵 | `/Users/zzhang/Documents/Shiba/docs/v2/TEST_MATRIX.md`; `/Users/zzhang/Documents/Shiba/scripts/test-fanout-recovery.sh`; `/Users/zzhang/Documents/Shiba/scripts/performance-matrix.py` | A | 旧矩阵仅作 evidence index；M2--M9 重证明 Runtime crash/CAS/operator 原子性，M10 重证明 production ingress feedback/restart/bounded transport，M11 重证明 bootstrap recovery 与 million-row RSS/throughput，M12 重证明 active rebuild recovery/governance/performance；不执行或移植旧脚本 | PG17.10/PG18.4 `scripts/release-matrix.sh` 汇总 48 个唯一脚本、96 次 PG 调用全绿；各 exact script 仍列于对应 milestone 行 | reconnect supervisor、network/TLS、cross-host soak、竞争 tail latency与 slot failover 未证明 |
| 已验证 failure cases | `/Users/zzhang/Documents/Shiba/docs/v2/REQUIREMENT_EVIDENCE.json`; `/Users/zzhang/Documents/Shiba/scripts/test-*-v2.sh` | A | 旧 negative-case registry 仅作 provenance；M2--M12 后续逐行只认 clean-room failure-first/rollback/crash/权限/边界测试，不以 legacy 通过替代新证据 | PG17.10/PG18.4：各 M2--M12 行所列 `scripts/test-m*.sh` 精确双版本命令；尤以 M3 crash、M7 invalidation、M8 CAS/bounds、M9 overflow、M10 no-ACK/drift、M11 recovery、M12 forward-only rebuild 为当前重证集合 | 未被具体 milestone 行和 clean-room 断言点名的 legacy failure case 仍未证明；slot failover、cross-host/process-kill 全窗口未证明 |
| 文档正确性不变量 | `/Users/zzhang/Documents/Shiba/docs/v2/INVARIANTS.md` | A | legacy 文档仅作 provenance；authority、writer、transaction、replay、ACK、bootstrap visibility 已在 clean-room contracts 独立重述，并由对应 Runtime/PG 门禁逐项约束 | PG17.10/PG18.4 `scripts/test-l0.sh` 加各 M2--M11 行所列 PG gate；当前契约入口为 `ARCHITECTURE.md`、`TRANSACTION_RECOVERY.md`、`SOURCE_INGRESS_CONTRACT.md`、`BOOTSTRAP_CONTRACT.md` 与本 manifest | 文档不替代 M12 rebuild、production supervisor、跨主机 soak 等尚无实现/测试的边界 |
| 可审计移植规则 | This clean-room decision; prospective sources must be recorded per change | B | No B implementation in Phase 1. A later commit must state source file, exact function/algorithm, deleted legacy dependencies, new boundary, clean-room equivalence evidence, LOC budget, and unproved boundary | Review + targeted clean-room test; no legacy test alone suffices | Every B candidate is unapproved until reviewed |
| M2 INSERT/count walking skeleton | 本 clean-room M2 执行契约；未读取或迁移旧实现、旧 SQL workflow 或旧 Runtime/Operator | 新实现（无 A/B/C 代码复用） | 独立定义 `SourceTransaction`、四个 durable facts 与单一 PostgreSQL transaction；旧 continuation/crash 条目仍只是 failure-case provenance | clean-room Runtime 单测；PG17/18 `scripts/test-m2.sh` | pgoutput、UPDATE/DELETE、并发、generation 切换、DDL、外部 effect 均未证明 |
| M3.1 live pgoutput INSERT | PostgreSQL `REL_17_STABLE`/`REL_18_STABLE` logical replication protocol、message format 与 `src/bin/pg_basebackup/pg_recvlogical.c`、`src/backend/replication/logical/proto.c`；检索于 2026-08-02；未读取旧实现 | 新实现（无 A/B/C 代码复用） | 独立实现严格 protocol-v1 `B/R/I+/C` decoder；live CLI capture 和 delimiter removal 仅存在于 test | PG17.10/PG18.4 `scripts/test-m3.sh`；官方文档与上述 PostgreSQL source | 旧 pgoutput failure-case fixture 仍 deferred；NULL、UPDATE/DELETE、TOAST、streaming、异常 transport restart 未证明 |
| M3.2 post-result/pre-ack crash replay | PostgreSQL `REL_17_STABLE`/`REL_18_STABLE` replication-slot/`pg_recvlogical` feedback semantics；检索于 2026-08-02；未读取旧实现 | A failure case，clean-room 重证 | test-only receiver 禁止周期 feedback，完整 COMMIT 后停止；M2 提交后 SIGKILL，再由同一 slot 重放 | PG17.10/PG18.4 `scripts/test-m3.sh`；`confirmed_flush_lsn` 前后断言、identity 相等、`AlreadyApplied` | production transport、主机掉电、slot 丢失/failover、streaming 未证明 |
| M4.1 nullable int8 INSERT | PostgreSQL `REL_17_STABLE`/`REL_18_STABLE` pgoutput tuple `n`/`t` message semantics；检索于 2026-08-02；未读取旧实现 | 新实现（无 A/B/C 代码复用） | 固定 key-only 与 key+nullable-int8 两种 admitted shape；现有 Apply fact 增加 presence/value，不建第二 authority | PG17.10/PG18.4 `scripts/test-m4.sh`；真实 NULL/42、精确坏 key tag、replay 与 SQL fact/result | empty tuple、composite identity、UPDATE/DELETE、replica identity、TOAST 未证明 |
| M4.2 empty INSERT tuple | PostgreSQL `REL_17_STABLE`/`REL_18_STABLE` zero-column RELATION/INSERT tuple semantics；检索于 2026-08-02；未读取旧实现 | 新实现（无 A/B/C 代码复用） | cause identity 区分空行；现有 Apply key 允许 NULL，不生成 synthetic row identity | PG17.10/PG18.4 `scripts/test-m4-empty.sh`；真实双空行、坏 column count、replay 与 SQL fact/result | composite identity、UPDATE/DELETE、replica identity、TOAST 未证明 |
| M4.3 composite int8 identity | PostgreSQL `REL_17_STABLE`/`REL_18_STABLE` two-column RELATION/INSERT semantics；检索于 2026-08-02；未读取旧实现 | 新实现（无 A/B/C 代码复用） | 固定双 int8 key；同一 Apply fact 增 key2 与 keyed-only pair uniqueness | PG17.10/PG18.4 `scripts/test-m4-composite.sh`；同 key1 双 row、坏 key2 tag、replay 与 SQL fact/result | UPDATE/DELETE、replica identity changes、TOAST、其他 composite shape 未证明 |
| M4.4 unchanged-key nullable payload UPDATE | PostgreSQL `REL_17_STABLE`/`REL_18_STABLE` UPDATE/new-tuple semantics；检索于 2026-08-02；未读取旧实现 | 新实现（无 A/B/C 代码复用） | 既有 Apply row 作为 current state 原位更新；不建 UPDATE log、第二 authority、adapter 或 fallback | PG17.10/PG18.4 `scripts/test-m4-update.sh`；真实 `U+N`、坏 key tag、continuation 后 backend termination、retry/replay 与 SQL state/result | DELETE、key-changing UPDATE、K/O old tuple、replica identity、TOAST、mixed same-row transaction 未证明 |
| M4.5 stable single-key DELETE | PostgreSQL [17 logical replication message formats](https://www.postgresql.org/docs/17/protocol-logicalrep-message-formats.html) 与 [18 logical replication message formats](https://www.postgresql.org/docs/18/protocol-logicalrep-message-formats.html) 的 protocol-v1 DELETE、`K` tuple 与 tuple-data semantics；检索于 2026-08-02；未读取或复用任何 legacy DELETE/decoder/runtime/SQL 实现 | 新实现（无 A/B/C 代码复用） | 仅接纳固定 relation OID、单列稳定 `int8` key 的 `D + K`；既有 `applied_insert` 作为唯一 current source-row state，DELETE 原位移除；同一 processor/PG transaction 写 count、result、continuation；不建 log、alias、adapter、fallback 或第二表 | PG17.10/PG18.4 `scripts/test-m4-delete.sh`；真实 `D + K`/count1/canonical text、坏 tuple tag、missing row、underflow、other-row isolation、continuation/crash/retry/replay 与 SQL state/count/result | `D + O`、replica identity `FULL`、composite DELETE、key-changing UPDATE、TOAST、streaming、generation change、多 source 未证明；该历史命名债务已由 M11.2 直接重命名且无兼容路径 |
| M4.6 replica identity fail-closed | PostgreSQL [17 logical replication message formats](https://www.postgresql.org/docs/17/protocol-logicalrep-message-formats.html) 与 [18 logical replication message formats](https://www.postgresql.org/docs/18/protocol-logicalrep-message-formats.html) 的 RELATION replica identity/key flags 与 DELETE `K`/`O` semantics；检索于 2026-08-02；未读取或复用 legacy admission/registry/runtime 实现 | 新实现（无 A/B/C 代码复用） | 精确接纳 default `d` 与四种冻结 shape 的 key flags；真实 FULL `f` + `D/O` 在 pure decode fail closed；不建 registry、binding state、adapter、fallback 或第二 authority | PG17.10/PG18.4 `scripts/test-m4-replica-identity.sh`；真实 default INSERT apply/replay 与 FULL DELETE 无写入、continuation 不推进 | FULL/INDEX/NOTHING admission、`D + O` decode、replica identity index drift、TOAST、DDL binding 与 production transport 未证明 |
| M5.1 unchanged TOAST text | PostgreSQL [17 logical replication message formats](https://www.postgresql.org/docs/17/protocol-logicalrep-message-formats.html) 与 [18 logical replication message formats](https://www.postgresql.org/docs/18/protocol-logicalrep-message-formats.html) 的 TupleData `u` unchanged-TOAST semantics；检索于 2026-08-02；未读取或复用 legacy TOAST/runtime/SQL 实现 | 新实现（无 A/B/C 代码复用） | 现有 current-state row 增 mutually-exclusive `payload_text`；真实 text INSERT 保存完整 UTF-8，UPDATE `u` 保留 durable text；同一 processor transaction 写 row/count/result/continuation，不查询 source 补值，不建第二 authority | PG17.10/PG18.4 `scripts/test-m5-toast.sh`；64 KiB external TOAST storage、真实 `U+N/t/u`、坏 tag、crash/retry/replay 与 exact SQL payload | new/incompressible TOAST value、NULL text、TOAST key、old tuple、schema binding drift、streaming 与 production transport 未证明 |
| M5.2 incompressible TOAST replacement | PostgreSQL [17 logical replication message formats](https://www.postgresql.org/docs/17/protocol-logicalrep-message-formats.html) 与 [18 logical replication message formats](https://www.postgresql.org/docs/18/protocol-logicalrep-message-formats.html) 的 TupleData text-value semantics，以及 PG17/18 `pg_column_compression`/default EXTENDED 行为；检索于 2026-08-02；未读取或复用 legacy TOAST/runtime 实现 | 新实现（无 A/B/C 代码复用） | Text UPDATE `t` 拥有完整 UTF-8 replacement；同一 processor 原位更新现有 `payload_text`，不读取 source/TOAST table，不建 cache、adapter、fallback 或第二 authority | PG17.10/PG18.4 `scripts/test-m5-incompressible-toast.sh`；两份 64 KiB seeded high-entropy value 均 out-of-line/uncompressed，真实 exact `U+N/t/t`、坏 binary tag、crash/retry/replay | NULL/binary text、TOAST key、old tuple、schema binding drift、streaming 与 production transport 未证明 |
| M5.3 composite-key DELETE | PostgreSQL [17 logical replication message formats](https://www.postgresql.org/docs/17/protocol-logicalrep-message-formats.html) 与 [18 logical replication message formats](https://www.postgresql.org/docs/18/protocol-logicalrep-message-formats.html) 的 multi-column key TupleData/DELETE semantics；检索于 2026-08-02；未读取或复用 legacy composite DELETE/runtime 实现 | 新实现（无 A/B/C 代码复用） | 现有 DELETE fact 携可选第二 int8 key；processor 以 existing current-state pair 精确删除，不建通用 identity abstraction、log 或第二 authority | PG17.10/PG18.4 `scripts/test-m5-composite-delete.sh`；真实 `D+K` count2/canonical pair、同 key1 isolation、坏 key2、missing pair、crash/retry/replay | composite UPDATE、其他 arity/type、replica identity INDEX/FULL、schema binding、streaming 与 production transport 未证明 |
| M5.4 replica identity index | PostgreSQL [17 logical replication message formats](https://www.postgresql.org/docs/17/protocol-logicalrep-message-formats.html) 与 [18 logical replication message formats](https://www.postgresql.org/docs/18/protocol-logicalrep-message-formats.html) 的 RELATION replica identity `i`、column key flag 与 DELETE `K` semantics；检索于 2026-08-02；未读取或复用 legacy registry/admission/runtime 实现 | 新实现（无 A/B/C 代码复用） | 显式 single-int8-key source constructor 选择 `i` admission；既有 decoder/processor 处理 INSERT 与 DELETE；default drift 在 pure decode fail closed；不建 catalog binding、通用 identity abstraction、adapter、fallback 或第二 authority | PG17.10/PG18.4 `scripts/test-m5-replica-index.sh`；真实 `R(i)`/key flag/`D+K`、atomic crash/retry/replay 与 live `R(d)` drift rejection | durable source/schema binding、composite replica index、FULL/NOTHING、`D+O`、无 RELATION 时 drift observation、streaming 与 production transport 未证明 |
| M5.5 name-independent source binding | PostgreSQL [17 logical replication message formats](https://www.postgresql.org/docs/17/protocol-logicalrep-message-formats.html) 的 RELATION OID/name fields与 [18 logical replication message formats](https://www.postgresql.org/docs/18/protocol-logicalrep-message-formats.html)，以及 PostgreSQL catalog OID identity；检索于 2026-08-02；未读取或复用 legacy source registry/binding/runtime 实现 | 新测试证据（无 A/B/C 代码复用） | 复用既有 explicit `PgoutputSource` relation-OID/shape/identity admission；名称只做 wire framing；不建 registry、discovery scan、adapter、fallback、alias 或第二 authority | PG17.10/PG18.4 `scripts/test-m5-source-binding.sh`；真实 schema/table/column rename 保持 OID 并继续 Apply/replay，同名 drop/recreate 新 OID 被旧 binding 在 pure decode 拒绝且 durable state 不变 | durable binding registration/lifecycle、ObjectAddress DDL observer、type/index drift、无 RELATION 时失效、streaming 与 production transport 未证明 |
| M6.1 streamed transaction commit | PostgreSQL [17 logical replication message formats](https://www.postgresql.org/docs/17/protocol-logicalrep-message-formats.html) 与 [18 logical replication message formats](https://www.postgresql.org/docs/18/protocol-logicalrep-message-formats.html) 的 protocol-v2 Stream Start/Stop/Commit/Abort 与 streamed XID fields；检索于 2026-08-02；未读取或复用 legacy streaming/ingress/runtime 实现 | 新实现（无 A/B/C 代码复用） | 独立 pure streamed-envelope decoder 复用冻结 relation/tuple admission；仅完整 single-XID key-only INSERT stream commit 产生既有 SourceTransaction；不建 spool、worker、transport、fallback 或第二 continuation | PG17.10/PG18.4 `scripts/test-m6-stream-commit.sh`；64 KiB decoding memory、真实 10,000-row multi-segment `S/R/I/E...c`、partial/corrupt/A rejection、crash/retry/replay 与 exact count | real streamed abort delivery/restart、interleaved XIDs/subtransactions、UPDATE/DELETE streaming、bounded memory/backpressure、production transport/feedback 未证明 |
| M6.2 real streamed abort/restart | PostgreSQL [17 logical replication message formats](https://www.postgresql.org/docs/17/protocol-logicalrep-message-formats.html) 与 [18 logical replication message formats](https://www.postgresql.org/docs/18/protocol-logicalrep-message-formats.html) 的 Stream Abort 与 slot feedback semantics；检索于 2026-08-02；未读取或复用 legacy receiver/recovery 实现 | 新测试证据（无 A/B/C 代码复用） | live test receiver observes E then real A before graceful stop；same slot restarts for independent commit；production decoder remains stateless and creates no abort value/spool/authority | PG17.10/PG18.4 `scripts/test-m6-stream-abort.sh`；real open transaction segments、ROLLBACK/A matching XID、zero durable state、feedback、same-slot restart、commit/apply/replay | crash before abort feedback、persisted partial-stream recovery、interleaving/subtransactions、production receiver lifecycle/backpressure 未证明 |
| M7.1 exact relation DDL invalidation | PostgreSQL [17 event-trigger functions](https://www.postgresql.org/docs/17/functions-event-triggers.html)、[17 event-trigger transaction behavior](https://www.postgresql.org/docs/17/event-trigger-definition.html)、[18 event-trigger functions](https://www.postgresql.org/docs/18/functions-event-triggers.html) 与 [18 explicit locking](https://www.postgresql.org/docs/18/explicit-locking.html)；legacy `docs/v2/INVARIANTS.md` 与 `scripts/test-ddl-invalidation-v2.sh` 仅作为 A 类 failure-case 来源；检索于 2026-08-02，未复用旧实现/SQL workflow | A failure case + clean-room 新实现 | immutable relation ObjectAddress binding 与独立 invalidation fact；单一 event-trigger writer；processor 在同一事务按 object lock→invalidation check→Apply 执行；不按名称匹配、不建兼容/回退路径 | PG17.10/PG18.4 `scripts/test-m7-ddl-invalidation.sh`；exact `(classid,objid,objsubid)`、unrelated isolation、DDL rollback、committed rename fail-closed、replay、ordinary permissions | rebuild lifecycle、production DDL operations 未证明 |
| M7.2 DROP/recreate/cascade invalidation | PostgreSQL [17 event-trigger functions](https://www.postgresql.org/docs/17/functions-event-triggers.html) 与 [18 event-trigger functions](https://www.postgresql.org/docs/18/functions-event-triggers.html) 的 `pg_event_trigger_dropped_objects()` ObjectAddress 语义；检索于 2026-08-02；未读取或复用 legacy DROP 实现或 SQL workflow | clean-room 新测试证据（无 A/B/C 代码复用） | 复用 M7.1 唯一 `sql_drop` event-trigger writer 与旧 ObjectAddress authority；不新增表、writer、runtime path 或 name fallback | PG17.10/PG18.4 `scripts/test-m7-drop-invalidation.sh`；direct DROP rollback、committed DROP、pending pre-Apply rejection、same-name/new-OID non-revival、schema CASCADE、historical replay | rebuild lifecycle 未证明 |
| M7.3 exact column DDL invalidation | PostgreSQL [17 event-trigger functions](https://www.postgresql.org/docs/17/functions-event-triggers.html) 与 [18 event-trigger functions](https://www.postgresql.org/docs/18/functions-event-triggers.html) 的 command ObjectAddress/`objsubid` 语义；检索于 2026-08-02；未读取或复用 legacy column/type binding 实现 | failing PG17 test + clean-room 新实现 | 同一 `source_binding` 正规化为 relation 与 live-column exact-address rows；同一 registration/event writer/processor transaction，不建第二 binding 表或 name fallback | PG17.10/PG18.4 `scripts/test-m7-column-invalidation.sh`；首次 column-rename test 先失败，修复后 exact subid、type rollback/commit、pending failure、state isolation、replay 全绿 | rebuild lifecycle 未证明 |
| M7.4 exact replica-identity index invalidation | PostgreSQL [17 event-trigger functions](https://www.postgresql.org/docs/17/functions-event-triggers.html)、[18 event-trigger functions](https://www.postgresql.org/docs/18/functions-event-triggers.html) 与 `pg_index.indisreplident`；检索于 2026-08-02；未读取或复用 legacy index binding/observer 实现 | clean-room 新实现（无 A/B/C 代码复用） | registration 在同一 binding set 冻结 current identity-index OID；closed binding kind 区分 relation/index；同一 event writer 与 processor transaction，不建 registry、第二表或 fallback | PG17.10/PG18.4 `scripts/test-m7-index-invalidation.sh`；exact binding set、unrelated isolation、rename rollback/commit、pending rejection、state isolation、replay | binding rebuild lifecycle 未证明 |
| M7.5 concurrent DDL/Apply lock order | PostgreSQL [17 explicit locking](https://www.postgresql.org/docs/17/explicit-locking.html) 与 [18 explicit locking](https://www.postgresql.org/docs/18/explicit-locking.html)；检索于 2026-08-02；未读取或复用 legacy scheduler/lock implementation | clean-room 新测试证据（无 A/B/C 代码复用） | test-only advisory trigger pauses existing processor after preflight；`pg_locks`/`pg_stat_activity` observe real relation lock order；不新增 production hook/worker/authority | PG17.10/PG18.4 `scripts/test-m7-concurrent-ddl.sh`；granted AccessShare、waiting AccessExclusive、zero blocked writes、Apply then DDL, next pending rejection/replay | binding rebuild lifecycle 未证明 |
| M8.1 multi-source union count | PostgreSQL row locks、MVCC 与 transaction rollback semantics；检索于 2026-08-02；未读取或复用 legacy multi-source scheduler/runtime 实现 | clean-room 新实现（无 A/B/C 代码复用） | relation-binding row is per-source mutex；fast replay + post-lock replay CAS；continuation ordering per source；existing singleton count/result explicitly aggregates source union；不新增表、worker、queue 或第二 writer | PG17.10/PG18.4 `scripts/test-m8-multi-source.sh`；two schemas/publications/slots、per-source continuation/replay、global result、source2 crash/retry、generation mismatch | stress/fairness、bounded backpressure、production receiver、performance budget 未证明 |
| M8.2 concurrent source CAS | PostgreSQL row-lock/wait-event/MVCC semantics；检索于 2026-08-02；未读取或复用 legacy concurrency scheduler | clean-room 新测试证据（无 A/B/C 代码复用） | test-only advisory trigger pauses existing Apply；same-source duplicate queues on binding mutex and post-lock replay CAS；different source progresses independently；不新增 production queue/hook | PG17.10/PG18.4 `scripts/test-m8-concurrent-sources.sh`；exactly one Applied/AlreadyApplied、zero blocked state、independent source commit、global count、per-source continuation/replay | stress/fairness、bounded ingress/backpressure、performance threshold 未证明 |
| M8.3 bounded pgoutput admission | M6.1 clean-room 10,000-row streamed transaction evidence 与 M5.1 64 KiB TOAST evidence；未读取或复用 legacy decoder/queue/runtime 实现 | clean-room 新实现（无 A/B/C 代码复用） | committed/streamed decoder 共用 16 MiB borrowed-input check，并各自在第 10,001 个 change decode/push 前 fail closed；不新增 queue、worker、authority 或依赖 | PG17.10/PG18.4 `scripts/test-m8-bounded-decode.sh`；真实 10,000 committed decode/apply/replay、10,001 rejection/state isolation；pure committed/streamed oversized rejection；M6 10,000 streamed regression | production receiver/transport memory、backpressure、吞吐和 heap peak 未证明 |
| M8.4 synchronous backpressure and performance budget | 本 clean-room M8.1–M8.3 row-lock/CAS、10,000-change 与无 Runtime queue 证据；legacy performance matrix 未读取或执行 | clean-room 新实现与新测试（无 A/B/C 代码复用） | 单一 10,000 transaction limit 由 constructor/decoder/processor 强制；同步 `process` 将 DB wait 传给 caller；冻结 2s decode/10s Apply/2s replay ceiling，不建 receiver、scheduler、queue 或 worker | PG17.10/PG18.4 `scripts/test-m8-performance.sh`；基线约 8ms/0.8s/0.2ms；10,001 constructor/forged-process rejection 与 durable isolation；M8.1 crash、M8.2 concurrency、M8.3 bounded input 组合证据 | production transport/backpressure、持续吞吐、heap peak、竞争 tail latency 与跨慢速硬件可移植性未证明 |
| M9.1 compiled CountRows contract | 本 clean-room M2–M8 fixed-count invariants；未读取或复用 legacy compiler/operator/runtime/SQL workflow | clean-room 新实现（无 A/B/C 代码复用） | 纯 Operator/Compiler；名称仅在 live descriptor 编译时解析为 ObjectAddress；Source Apply 生成不落表的 EffectBatch；按 operator_id 锁序更新通用 state/result；旧 count authority 直接删除，无 alias/dual-write | pure compiler/operator tests；PG17/18 `scripts/test-m9-registration.sh` 与迁移后的 M2–M8 gates | SumInt8 端到端、production receiver、binding rebuild、非聚合 operator、持续吞吐/heap peak/tail latency 未证明 |
| M9.2 CountRows + SumInt8 transaction | 本 clean-room M8.4 10,000-change performance evidence 与 M9.1 EffectBatch/ordered operator contract；未读取或复用 legacy operator/runtime/SQL workflow | clean-room 新实现与新测试（无 A/B/C 代码复用） | 一个 live nullable-int8 batch 依序驱动 CountRows 与 ObjectAddress-bound SumInt8；只补足该 relation 的真实两位置 `D + K`（`t` key + `n` placeholder）；沿用同一 processor transaction、state/result authority 与 2s/10s/2s ceiling；不建 effect table、SQL execution、worker 或第二 writer | PG17/18 `scripts/test-m9-count-sum.sh`、`scripts/test-m9-operator-concurrency.sh`、`scripts/test-m9-operator-performance.sh`；固定 count=10000/sum=15000；PG17 decode 9.56ms/Apply 836.72ms/replay 0.171ms；ordered operator-2 failure 全回滚 | production receiver/restart、persisted partial-stream recovery、binding rebuild、SQL frontend、非聚合 operator、nullable-payload streaming、sustained throughput、heap peak、tail latency 未证明 |
| M10.1 committed production ingress | PostgreSQL [17 streaming replication protocol](https://www.postgresql.org/docs/17/protocol-replication.html)、[18 streaming replication protocol](https://www.postgresql.org/docs/18/protocol-replication.html) 与 PG17/18 logical-replication terminal message formats；检索于 2026-08-02；只读审计现有 Rust client API，未读取或复用 legacy ingress/runtime/SQL workflow | clean-room 新实现（无 A/B/C 代码复用） | safe Rust `libpq` binding 承担 startup/auth/frontend-backend framing/COPY BOTH；独占 wrapper 不 clone/外泄底层 Connection；Shiba 仅解析 w/k envelope、结构 frame 与 bounded one-transaction assembly，现有 Runtime decoder 仍是唯一 semantic decoder；独立 Apply connection同步反压，无 CLI/queue/第二 authority | pure envelope/每字节 split/coalesced/16MiB tests；PG17.10/PG18.4 `scripts/test-m10-committed-ingress.sh` 真实 COPY BOTH→existing decoder/Runtime→CountRows=2、SumInt8=10、continuation=1 | protocol-v2 production assembly、slot/config/generation、least privilege、disconnect/TLS、sustained performance 尚未证明；libpq binding 的底层 Connection Clone soundness 缺陷由 private exclusive wrapper 禁用但仍属依赖维护风险 |
| M10.2 durable feedback/restart | PostgreSQL [17 streaming replication protocol](https://www.postgresql.org/docs/17/protocol-replication.html)、[18 streaming replication protocol](https://www.postgresql.org/docs/18/protocol-replication.html) 的 standby status 与 pgoutput COMMIT end-LSN；REL_17/REL_18 walsender/logical/pgoutput source semantics；检索于 2026-08-02；未读取 legacy feedback/recovery 实现 | clean-room 新实现与 failure-first live evidence（无 A/B/C 代码复用） | explicit receive→Apply→pending-token→ACK 状态机；write/flush/apply=end_lsn only after Applied/AlreadyApplied；requested keepalive只回 last durable；decoder/Runtime failure poison；slot transport authority与continuation compute authority分离，无LSN镜像/队列 | 首个PG17 live test证明仅PQput不推进slot后加入explicit flush；PG17.10/PG18.4 `scripts/test-m10-committed-ingress.sh` 证明pre-Apply/pre-feedback crash、AlreadyApplied、exact confirmed_flush、decoder/operator no-ACK、retry与real requested keepalive old-LSN reply | protocol-v2 streamed commit/abort production feedback、slot/config/generation、least privilege、network disconnect/TLS、sustained performance 尚未证明 |
| M10.3 protocol-v2 production streaming | PostgreSQL [17 logical replication message formats](https://www.postgresql.org/docs/17/protocol-logicalrep-message-formats.html)、[18 logical replication message formats](https://www.postgresql.org/docs/18/protocol-logicalrep-message-formats.html) 与 streaming replication XLogData semantics；检索于 2026-08-02；未读取或复用 legacy streaming/receiver/runtime/SQL 实现 | clean-room 新实现与 failure-first live evidence（无 A/B/C 代码复用） | production COPY BOTH 增 bounded protocol-v2 structural assembly，完整 change commit 仍交 existing Runtime decoder；partial/E 无 Apply/ACK；abort 使用其完整 frame 所在 outer XLogData `dataStart`；strict empty commit 以常数状态仅接纳 exact `S(true)E(S(false)E)*c` 并显式 ACK；legal R/I 必须走 sole decoder；closed authorization set 为 Applied/AlreadyApplied/EmptyCommitted/Aborted；无 spool、queue、第二 decoder/continuation | 首次 PG17 live gate 暴露真实 multi-segment publication-empty commit 并使旧 single-segment 假设失败；修正后 PG17.10/PG18.4 `scripts/test-m10-streaming-ingress.sh` 证明 real segment_count > 1、terminal 前无 ACK、空流重放/exact ACK、无 durable mutation/反馈循环、后续 source Apply、abort、partial crash/restart、16 MiB 与 10,000/10,001 边界；pure every-split/逐字节 chunk 与负向 grammar 绿色 | empty structure 只证明 process-selected publication 输出为空；publication identity/drift、slot/config/generation、least privilege、disconnect/TLS、sustained performance 仍未证明；ALTER/DROP/remove-readd/same-name recreate 必须由 M10.4 持久失效或权限禁止 |
| M10.4 source ingress configuration authority | PostgreSQL [17 event-trigger functions](https://www.postgresql.org/docs/17/functions-event-triggers.html)、[18 event-trigger functions](https://www.postgresql.org/docs/18/functions-event-triggers.html)、`pg_publication`/`pg_publication_rel`/`pg_replication_slots` catalog semantics；检索于 2026-08-02；未读取或复用 legacy ingress/catalog/slot workflow | clean-room 新实现与 failure-first live evidence（无 A/B/C 代码复用） | 单一 config 冻结 database、exact publication OID、name/flags/normalized attnums snapshot、existing slot/generation；同一 DDL writer 持久 publication drift；`pg_replication_slots` 保持 physical cursor authority且无 LSN mirror；pristine-only existing-slot CAS rotation，不 create/drop/alias/fallback | 首次 PG17 `ALTER PUBLICATION ... DROP TABLE` gate 证明 `pg_event_trigger_ddl_commands()` 无 command row，促成 exact OID+snapshot scan；PG17.10/PG18.4 `scripts/test-m10-catalog-ingress.sh` 证明 rollback/commit、remove-add、drop/same-name recreate、slot validation、stale CAS、PUBLIC denial和 slot set 不变；M10 后续行证明 governed ingress，M12 行证明 non-pristine rebuild | network/TLS、automatic reconnect supervision、cross-host sustained performance 未证明 |
| M10.4 governed receiver lifecycle and roles | PostgreSQL session advisory locks、logical-slot active state、published-table and replication-role permissions；检索于 2026-08-02；未读取或复用 legacy receiver/role/operations workflow | clean-room 新实现与 PG17/18 差分证据（无 A/B/C 代码复用） | 每 source exact 2 connections，process cap 32；attach inactive→COPY BOTH→active revalidation；receive/Apply/all ACK 连续 revalidate；Apply NOSUPERUSER/NOREPLICATION 与 receiver NOSUPERUSER/REPLICATION 分权；等待 WAL 无 Apply transaction；无 pool/auto slot/LSN mirror | PG17.10/PG18.4 `scripts/test-m10-governed-ingress.sh`；wrong role/generation、active/advisory conflict、detach/reattach、1 Apply+1 replication connection、10,000 streamed least-privilege Apply/ACK、publication remove-add 后 pending Empty ACK 拒绝且 LSN/result 不变；pure 32-source/conninfo/timeout gates | 本切片未证明的 idle blocked-receive cancellation 与 final local performance 已由下一行补证；TLS、disconnect/async COPY errors、Apply-time shutdown、reconnect daemon/backoff、non-pristine rebuild 仍未证明 |
| M10 final bounded transport, shutdown, and performance | PostgreSQL/libpq asynchronous `PQgetCopyData`、socket poll/consume-input semantics与本 clean-room M8/M10 bounded/backpressure failure cases；检索于 2026-08-02；未读取或复用 legacy receiver/performance/shutdown 实现 | clean-room 新实现与 PG17/18 failure-first evidence（无 A/B/C 代码复用） | 先 drain libpq buffered CopyData 再 poll；cooperative idle shutdown；constant-size connection-local relation proof；CopyData Vec≤16MiB+25、assembly≤16MiB、changes≤10000、one outstanding/no queue | PG17.10/PG18.4 `scripts/test-m10-performance-ingress.sh`：true 10k source-commit→durable-Apply 860.865/867.479ms，replay 29.350/31.085ms；100 precommitted service 160.52/135.26tx/s，p99 6.533/7.776ms；`scripts/test-m10-shutdown-ingress.sh` idle stop 42.262/76.950ms≤1s、no ACK/state/LSN、detach/reattach；pure first/repeated/omitted/mismatch R | TLS/disconnect、shutdown during Apply、reconnect/backoff、allocator/RSS、cross-host soak、non-pristine rebuild 仍未证明 |
| M11.1 consistent bootstrap contract | PostgreSQL [17 logical-decoding exported snapshots](https://www.postgresql.org/docs/17/logicaldecoding-explanation.html)、[18 replication protocol `EXPORT_SNAPSHOT`](https://www.postgresql.org/docs/18/protocol-replication.html) 与 [17 `SET TRANSACTION SNAPSHOT`](https://www.postgresql.org/docs/17/sql-set-transaction.html)；检索于 2026-08-03；未读取或复用 legacy bootstrap/slot/snapshot 实现 | clean-room 新契约与 test-only libpq experiment（无 A/B/C 代码复用） | 唯一边界为 new slot `EXPORT_SNAPSHOT` 返回的 `consistent_point`+ephemeral snapshot；独立 BootstrapId/BatchId 与一个 checkpoint authority；短 RR/RO import；hidden pristine pre-scan reset；same-slot post-scan catch-up；building result；无 fake WAL/continuation/spool | PG17.10/PG18.4 `scripts/test-m11-bootstrap-contract.sh`：exact four-field response、repeat import、concurrent I/U/D SQL differential、slot confirmed=consistent、next-command expiry、existing import stable、no Shiba writes；PG18 额外暴露 hex snapshot token 差异 | Production scanner/recovery/performance 已由 M11.2–M11.4 补证；active/non-pristine rebuild 属 M12 |
| M11.2 production snapshot-to-live vertical | M11.1 clean-room contract、PostgreSQL exported snapshot/logical message semantics 与 M9/M10 Runtime/Ingress contracts；检索于 2026-08-03；未读取或复用 legacy bootstrap/runtime/SQL workflow | clean-room 新实现与 PG17/18 SQL differential（无 A/B/C 代码复用） | 直接以 `source_row_state` 替换旧 cause-shaped table；strong BootstrapId/BatchId 与 closed EffectOrigin；单一 `source_bootstrap`；≤10000 row set-based batch；building/NULL sink；attempt-bound committed-message fence；复用 M10 receiver/ACK/live session；无 fake transaction、第二 continuation、WAL spool 或 durable effects | PG17.10/PG18.4 `scripts/test-m11-bootstrap.sh`：batch=2 baseline private 3/40、concurrent INSERT/UPDATE/DELETE catch-up 3/25、exact fence active 3/25、SQL exact rows、WAL-only continuation/slot ACK、ordinary live INSERT to 4/32；无 partial/duplicate | Recovery/performance 已由 M11.3/M11.4 补证；M12 未触碰 |
| M11.3 explicit pristine recovery | M11.1/M11.2 clean-room snapshot contract、PostgreSQL persistent logical-slot/advisory-lock/transaction semantics；检索于 2026-08-03；未读取或复用 legacy bootstrap/recovery/slot workflow | clean-room 新实现与 PG17/18 failure-matrix evidence（无 A/B/C 代码复用） | exact pre-scan attempt+slot+generation replacement；transport先删除 exact inactive slot，单一 SQL writer 原子清理 hidden rows/state并复用 live reservation validator；post-scan same-slot resume；exact fence marker+activation_end_lsn 恢复 active-before-feedback；无 auto slot、第二 cursor/continuation、alias 或 fallback | PG17.10/PG18.4 `scripts/test-m11-recovery.sh`：durable creating/slot-absent 重构后 cleanup+replacement、partial reset、stale/foreign reject、batch replay、overflow rollback、worker conflict、scan_complete+PG restart、Apply/active cutover killed-ACK replay、feedback-covered restart、SQL differential 4/50；active rebuild 由 M12.4 重证 | 未直接注入 reservation 指令级 process kill 或 active foreign old-slot conflict；production recovery supervisor/reconnect policy 未证明 |
| M11.4 bounded million-row bootstrap | Clean-room M11.2/M11.3 synchronous batch/catch-up contracts；2026-08-03 冻结阈值后运行；未读取或复用 legacy bootstrap/performance workflow | clean-room 新性能与 SQL differential evidence（无 A/B/C 代码复用） | 1,000,000 snapshot rows/100x10,000 bounded batches；one exact 10,000-change concurrent WAL transaction；三连接扫描、同步 direct backpressure、无 queue；阈值 120s、>=10k rows/s、catch-up<=15s、RSS<=256MiB | PG17.10 `scripts/test-m11-bootstrap-performance.sh`：3.098397625s/322,747.47 rows/s，catch-up 1.320857542s，RSS 10,160→13,824 KiB；PG18.4：3.136067542s/318,870.68 rows/s，1.329330584s，10,160→13,824 KiB；SQL differential/live handoff green；active rebuild 由 M12.6 重证 | indefinitely sustained concurrent writers/tail latency、reconnect supervisor、cross-host soak 未证明；M11 在声明范围完成但 V2 未完成 |
| M11.5 least-privilege bootstrap roles | M10.4 clean-room split-role contract 与 M11.2--M11.3 bootstrap control/scan/catch-up lifecycle；未读取或复用 legacy role/permission workflow | clean-room 新测试证据（无 A/B/C 代码复用） | NOSUPERUSER/NOREPLICATION control+Apply 与 scanner 执行 reservation、snapshot scan、catch-up 和 activation；独立 NOSUPERUSER/REPLICATION transport 只拥有复制连接；reader 只读公开 result；角色职责不合并，不新增 authority、fallback 或 owner/superuser 路径 | PG17.10 `./scripts/test-m11-bootstrap-roles.sh /opt/homebrew/opt/postgresql@17/bin/pg_config` 与 PG18.4 `./scripts/test-m11-bootstrap-roles.sh /opt/homebrew/opt/postgresql@18/bin/pg_config`：完整 non-superuser bootstrap→catch-up→activation；control/scanner/transport/reader 角色互换拒绝；缺 `EXECUTE`/source `SELECT`/internal `UPDATE` 分别 fail closed | `restart_abandoned` 的 split-role success、TLS/密码认证、跨主机凭据管理与列级 source 权限尚未证明；M12 active/non-pristine rebuild 未触碰 |
| M12.1 offline active-source rebuild contract | PostgreSQL 17.10/18.4 `pg_replication_slots` observable fields and this clean-room M10/M11 authority/recovery evidence; no legacy rebuild, SQL workflow or slot implementation read or reused | clean-room new contract and failure-first evidence (no A/B/C code reuse) | Freeze exact-old CAS; target becomes sole building authority at destructive prepare; old compute retires and recovery is forward-only; activation promotes same authority; observable slot drift rejects; trusted `REPLICATION` capability and undetectable identical privileged replacement recorded as excluded residual risk; no candidate, second bootstrap/continuation, birth marker, alias or fallback | PG17.10/PG18.4 `scripts/test-m12-rebuild-contract.sh`: real active/non-pristine M11/M10 source; pristine rotation/replacement, stale worker and preoccupied target reject with all authority unchanged; same-name/same-shape slot recreation exposes identical stable fields; Markdown/static gates; production path subsequently proved by M12.2--M12.6 rows | M12 declared boundary complete; privileged indistinguishable slot replacement remains excluded |
| M12.2 active-source rebuild admission and identity correction | This clean-room M12.1 contract plus PostgreSQL 17.10/18.4 relation, publication, replica-identity, role and logical-slot catalogs; no legacy rebuild implementation, SQL workflow or authority read or reused | clean-room new implementation and failure-first PG evidence (no A/B/C code reuse) | Sole prepare writer performs side-effect-free target/current validation and exact-old CAS, then installs target `rebuild_prepared`, building/NULL results and retired computation. Pre-M12 old authority is exact relation+2 columns; M12 target/produced authority adds the exact primary-index ObjectAddress as a fourth row in the same `source_binding`. The retired triple persists across phases, selects the generation shape, and is overwritten by the next rebuild; recovery cannot discover/substitute another index | PG17.10/PG18.4 `scripts/test-m12-rebuild-admission.sh` proves admission rollback/single winner. Failure-first iterations exposed `session_user`, bigint literal, PL/pgSQL block-label, deferred-constraint qualification and PG17 unparenthesized `IF CASE`; corrected PG17.10/PG18.4 `scripts/test-m12-rebuild-identity-authority.sh` proves exact-four persistence, catalog-only resume, rename/isolation, binding corruption rejection, repeat rebuild CAS, and same-shaped replacement-OID rejection | M12.3--M12.6 subsequently prove data, recovery, governance and release; same-OID rename is narrow reconcile, replacement OID fails closed; same-name/same-shape privileged slot replacement remains excluded residual risk |
| M12.3 active rebuild snapshot-to-live | Clean-room M12.1/M12.2 contracts plus proved M10 receiver/ACK and M11 exported-snapshot/bounded bootstrap paths; no legacy rebuild, slot or SQL workflow read or reused | clean-room new orchestration and PG17/18 SQL differential evidence (no A/B/C code reuse) | Active non-pristine generation 2 prepares generation 3 exact-four authority; exact inactive old slot is dropped, target slot supplies real `EXPORT_SNAPSHOT`, existing bounded scanner consumes baseline, and M10 consumes concurrent I/U/D WAL through fence, activation and live ACK. Binding-lock-first validation admits ordinary WAL only in catching_up/active; results remain building/NULL until activation; old continuation is not copied; identity is not switched twice; retired triple persists; old token/attach reject | PG17.10/PG18.4 `scripts/test-m12-rebuild-snapshot-live.sh` green: real slot/snapshot, bounded scan, concurrent INSERT/UPDATE/DELETE catch-up, SQL-equal rows and CountRows/SumInt8, phase/generation rejection, atomic visibility, generation-3 live transaction and ACK | M12.4--M12.6 subsequently prove recovery/governance/release; privileged indistinguishable slot replacement remains excluded residual risk |
| M12.4 forward-only rebuild recovery | Clean-room M11 abandoned-attempt and M12.1--M12.3 lifecycle contracts plus PostgreSQL exported-snapshot/slot persistence semantics; no legacy rebuild, slot or recovery workflow read or reused | clean-room extension of the sole lifecycle writer and existing M11/M10 recovery paths; no second bootstrap, continuation or decoder | `rebuild_prepared` resumes exact handoff. A lost M12 `creating`/`scanning` snapshot is abandoned, partial state resets transactionally, and the same exact-four target authority advances through a fresh BootstrapId, distinct slot and exact successor generation. Marker-null M11 recovery is unchanged; old authority cannot return | PG17.10/PG18.4 `scripts/test-m12-rebuild-recovery.sh`: deterministic slot/snapshot/scan/catch-up/fence/activation/ACK crash boundaries, SQL differential, lifecycle/visibility/checkpoint/LSN assertions and retry; exact green results recorded with M12.4 commit | M12.5/M12.6 subsequently prove governance/release; identical privileged same-name/same-shape slot replacement remains excluded residual risk |
| M12.5 rebuild governance and least privilege | PostgreSQL 17.10/18.4 catalogs and locking semantics for `IDENTIFY_SYSTEM`, relation/index ObjectAddress, `pg_relation_size`, publication/replica identity, advisory locks and role privileges; no legacy rebuild/role/SQL workflow read or reused | clean-room production preflight plus failure-first PG governance evidence; reuses existing binding, lifecycle, ingress and role authorities without a table, cursor, marker, alias or fallback | Side-effect-free transport `IDENTIFY_SYSTEM`+database and control target-`SELECT` preflight precede destructive prepare. Exact relation/publication/key/payload/primary-index/operator-plan ObjectAddresses are revalidated; exact index is held with `AccessShareLock` while `pg_relation_size` checks shape. Control/Apply/scanner is `NOREPLICATION`; transport is trusted `REPLICATION` with target `SELECT`; reader is public-result-only. Config/operator reads avoid needless row-update locks; same-source ownership yields one rebuild winner while other sources progress | PG17.10/PG18.4 `scripts/test-m12-rebuild-governance.sh`: relation/publication/PK ObjectAddress drift, replica-identity/column/operator-plan rejection, same-OID rename/unrelated isolation, post-prepare invalidation building/NULL, one-winner/different-source progress, split-role success and permission/role-exchange rollback | M12.6 subsequently proves bounded release; TLS/reconnect/cross-host operations and privileged indistinguishable same-name/same-shape slot replacement remain unproved/excluded residual risk |
| M12.6 bounded active rebuild and release matrix | Clean-room M11 bounded-bootstrap evidence and M12.1--M12.5 sole-authority rebuild path; no legacy performance script, rebuild workflow or threshold reused | clean-room performance/differential/release evidence; thresholds frozen before observation, with no production authority or path added | One million active rows, nonzero operator state and continuation; existing 10,000-row scanner batches plus one real 10,000-change WAL transaction; fixed limits scan <=12s, catch-up <=8s, activation <=2s, total <=25s, RSS delta <=128MiB, retained WAL <=256MiB; compare M11 ~3.1s/~1.3s/~3.6MiB; fixed-order complete PG17/18 release matrix | PG17.10/PG18.4 `scripts/release-matrix.sh`: 48 unique scripts, 96 PG invocations, all green. `scripts/test-m12-rebuild-performance.sh`: scan 4.357951916/4.429333333s, catch-up 1.946769416/1.907849875s, activation 9.755875/9.981958ms, total 6.343139667/6.375927458s, RSS +4272/+4320KiB, retained WAL 252864952/252898072 bytes | M12 is complete at the declared active nullable-int8 CountRows/SumInt8 boundary. SQL frontend, broader source/operator/result shapes, TLS/reconnect/cross-host/failover/soak/tail-latency and privileged indistinguishable slot replacement remain unproved/excluded |
| M13.1 generic Operator Kernel contract | M9--M12 clean-room Operator/Compiler/Runtime/Ingress behavior and current-code specialization audit; no legacy operator, SQL workflow or authority read or reused | clean-room design freeze and independently measured same-machine baseline (no A/B/C code reuse) | Freeze canonical compiled plan/digest, opaque state, typed scalar/keyed transition, bounded ProjectRows, sole registration/Runtime writers, fixed lock/commit/ACK order and target-plan installation at M12 destructive prepare; no candidate, adapter, dual write or persisted effects | `docs/OPERATOR_KERNEL_CONTRACT.md`; ADR 0004; five PG17.10/PG18.4 runs of `scripts/test-m9-operator-performance.sh`: Apply medians 768.727625/770.174125 ms and 15% ceilings 884.036769/885.700244 ms; M13.1 docs/L0 gates | Production kernel, generic Catalog/Runtime, ProjectRows SQL differential, bootstrap/rebuild integration and final PG17/18 matrix remain for M13.2--M13.5 |
| M13.2 pure generic Operator Kernel | M13.1 clean-room contract and M9 CountRows/SumInt8 reference invariants; no legacy operator/compiler implementation or SQL workflow read or reused | clean-room new pure Rust codecs, compiler and reference-model evidence (no A/B/C code reuse) | One canonical `CompiledPlan`/digest, opaque versioned state and typed transition/output contract serve CountRows, SumInt8 and ProjectRows; concrete dispatch stays inside `shiba-operator`; NULL differs from absent; checked arithmetic, corrupt codecs and 20,000 keyed-mutation bound fail closed; no PostgreSQL dependency, SQL, persisted effects or second Runtime | `cargo test -p shiba-operator -p shiba-compiler`; strict IR/canonical digest tests, randomized I/U/D differential, NULL/absent, key-changing ProjectRows, overflow/corruption/amplification gates; green commit `d41aa1b` | PostgreSQL persistence and lifecycle integration were subsequently assigned to M13.3/M13.4; broader types, expressions, joins/windows/DAG remain excluded |
| M13.3 generic Catalog, Runtime and Result Sink | M13.1/M13.2 clean-room contracts plus M9 transaction/lock/replay invariants; no legacy Catalog, Runtime, SQL workflow or result sink read or reused | clean-room schema replacement, generic Runtime persistence and PG17/18 differential evidence (no A/B/C code reuse) | Sole definition row stores strict spec, canonical plan/digest, state codec and scalar/keyed output contract; Runtime decodes plan/state once per operator, locks by operator ID, computes one transition and persists opaque state plus scalar replacement or bounded keyed mutations. ProjectRows uses the sole Source Apply/EffectBatch path, including narrow `U+K+N` key change. Active-only public keyed view prevents building leakage; no concrete SQL kind constraint, adapter, dual write or per-row round trip | PG17.10/PG18.4 `scripts/test-m13-operator-kernel.sh`: full keyed SQL oracle for I/U/key-change/D/NULL, exact replay, corrupt plan/state and sink rollback; migrated `scripts/test-m9-registration.sh` and `scripts/test-m9-count-sum.sh` green on both; Catalog unit/clippy and PG17/18 empty-install rollback green; commit `cc6960e` | M13.4 subsequently removed fixed IDs/counts/positions and M13.5 closed release evidence; broader types and operator families remain excluded |
| M13.4 generic lifecycle integration | M10--M12 clean-room ingress, snapshot, ACK and forward-only rebuild contracts plus M13.3 durable ordered plan set; no legacy lifecycle or SQL workflow read or reused | failure-first clean-room de-specialization and PG17/18 regression evidence | Bootstrap/Rebuild/Ingress load the complete Catalog plan set in operator-ID order, never accept/rebuild fixed Count/Sum IDs, assume cardinality, infer payload sub-ID or branch on kind; Runtime's sole transaction-local registration writer rebinds and resets plans at destructive prepare/recovery; live, snapshot, catch-up and rebuild retain one Runtime/ACK/authority path | PG17.10/PG18.4 committed/streaming ingress, bootstrap/recovery/roles, rebuild contract/admission/snapshot-live/recovery/identity/governance; full ProjectRows keyed SQL oracle; failure-first corrupt old plan digest rejection before destructive prepare and partial keyed recovery reset; forbidden-specialization scan green; commit `22251f0` | M13.5 subsequently closed release/performance evidence; broader row order/types, expressions, joins/windows/DAG remain excluded |
| M13.5 complete generic-kernel release evidence | M13.1 frozen same-machine baseline and M1--M13 clean-room gates; no legacy performance, test harness or operator implementation read or reused | clean-room release, performance, static and documentation evidence (no A/B/C code reuse) | One release runner retains exact enrollment of all test scripts; historical M12 million-row CountRows+SumInt8 scenario remains frozen so keyed result WAL cannot silently redefine its 256 MiB baseline; ProjectRows lifecycle remains independently proved with full-row SQL oracles; no threshold or assertion is relaxed | `scripts/release-matrix.sh` on PG17.10/PG18.4: 49 unique scripts, 98 invocations, all green. Five-run post-change CountRows+SumInt8 Apply medians 782.302750/787.157125 ms versus 768.727625/770.174125 ms baseline (+1.8%/+2.2%, below 15%). Final M12 retained WAL 252876464/252917880 bytes below 256 MiB. Forbidden production specialization scan empty | M13 completes the declared nullable-int8 CountRows/SumInt8/ProjectRows generic-kernel boundary, not V2. SQL frontend, broader types/results, joins/windows/DAG, TLS/reconnect, cross-host/failover/soak/tail latency remain unproved |
| M14.1 typed Operator SDK and graph contract | M13 clean-room generic kernel/schema/runtime evidence and current-code specialization audit; PostgreSQL 17.10/18.4 transaction/lock semantics; no legacy operator, join, graph, SQL workflow or authority read or reused | clean-room design freeze and authority audit (no A/B/C code reuse) | One canonical graph owns exact sources/nodes/edges/terminal outputs; typed transaction-local rows/deltas; generic keyed state; graph/generation continuation; one-slot two-source transaction and exported-snapshot/rebuild boundary; fixed lock order, fail-closed bounds and performance lines; ProjectRows must become Project+Materialize without adapter | `docs/OPERATOR_GRAPH_CONTRACT.md`; ADR 0005; clean baseline `666a423`; M13 49-script/98-invocation evidence retained; Apply medians 782.302750/787.157125 ms and M14 15% lines 899.648163/905.230694 ms; M14.1 docs/L0 gates | M14.2--M14.7 subsequently closed expressions, grouping, join, graph lifecycle and PG17/18 release evidence; SQL parser, outer/three-table join, window/DISTINCT/Min/Max/Avg/plugins/scheduler remain excluded |
| M14.2 typed stateless graph and Materialize | M14.1 clean-room contract plus M13 ProjectRows full-row differential and transaction invariants; no legacy graph/operator/SQL implementation read or reused | clean-room pure Rust implementation and vertical PG regression evidence (no A/B/C code reuse) | Canonical binding-ordered typed layout, strict expressions, Filter/Compute/Project, bounded Materialize deltas and one shared Runtime DeltaBatch replace the ProjectRows variant; concrete dispatch remains inside `shiba-operator`, intermediate batches are transaction-local, and Runtime retains sole state/result writer | `cargo test -p shiba-operator -p shiba-compiler`; fixed-seed keyed reference differential, NULL/absent/overflow/layout/digest and hard-bound tests; PG17.10/PG18.4 `scripts/test-m13-operator-kernel.sh`; workspace/clippy/L0 and forbidden ProjectRows production scan | M14.3--M14.7 subsequently closed generic grouping, two-source join, graph lifecycle and release/performance evidence |
| M14.3 generic grouped state and aggregates | M14.1/M14.2 clean-room contracts plus M13 Runtime transaction/replay invariants; no legacy grouped operator, state SQL or workflow read or reused | clean-room generic state implementation and PG17/18 full-row differential evidence (no A/B/C code reuse) | The then-current `operator_node_state` authority used canonical plan/operator, node, namespace and typed partition/item keys; M14.6 later replaced it in place with graph-scoped `graph_node_state`. KeyBy, GroupedCount and GroupedSumInt8 share generic read/snapshot/delta contracts; Runtime persists state/results set-wise with typed NULL and no per-row SQL | Catalog 23-test PG17/PG18 builds; PG17.10/PG18.4 `scripts/test-m14-grouped.sh`: full SQL oracle, I/U/D, NULL/key/group transitions, empty-group deletion, overflow/corrupt-state rollback, retry/replay, permissions and set-based static gates | M14.4--M14.7 subsequently closed Join, graph lifecycle/bootstrap/rebuild and full release/performance evidence |
| M14.4 accepted two-source JOIN authority | M14.1 graph decision, M10 single-slot transaction/ACK evidence, M11 exported-snapshot evidence, M12 forward-only rebuild evidence and M14.3 generic keyed state; no legacy JOIN, multi-source runtime, SQL workflow or authority read or reused | clean-room contract and ADR only (no A/B/C implementation reuse) | Freeze exactly two explicit SourceIds in one database/publication/slot/generation; one atomic pgoutput graph transaction, graph continuation/ACK, exported snapshot and graph-wide rebuild; exact relation/column plus right bigint PK/UK index ObjectAddresses; fixed graph→replay→source→row→state lock order and bounded set-based right fan-out; explicitly forbid per-source continuation, second Runtime, persisted DeltaBatch, adapter/fallback/dual write | `docs/JOIN_AUTHORITY_CONTRACT.md`, ADR 0006, OPERATOR_GRAPH/ARCHITECTURE/TESTING/GOAL_GAP/README consistency and Markdown/static diff review | M14.5--M14.7 subsequently implemented and proved JOIN node/Runtime/Catalog/lifecycle, PG17.10/18.4 admission, SQL differential, crash/replay/DDL, least privilege, bootstrap/rebuild and frozen fan-out/performance evidence |
| M14.5 pure multi-input INNER JOIN kernel | M14.4 clean-room authority plus M14.2 typed graph and M14.3 generic partition-state contracts; no legacy JOIN/compiler/runtime/SQL workflow read or reused | clean-room pure Rust implementation and deterministic differential evidence (no A/B/C code reuse) | Nonzero GraphId owns ordered SourcePorts; `SourcePort(SourceId)` binds inputs; Compiler requires the exact effective right replica-identity ObjectAddress; generic partition state computes both inputs from pre-state to final-state. Ordered membership replaces the initial quadratic affected-row scan with `O(n log n)` behavior | `cargo test -p shiba-operator -p shiba-compiler`: strict identity/descriptor gates, mixed changes, NULL/corrupt state, fixed-seed 300-step relational differential, exact fan-out 20,000 accepted and 20,001 rejected | M14.6/M14.7 subsequently closed Runtime/Catalog, cross-schema live execution, graph continuation/ACK, bootstrap/rebuild/recovery and release/performance evidence |
| M14.6 unified graph production cutover | M10--M13 clean-room transport, transaction, bootstrap and rebuild invariants plus the M14.1--M14.5 clean-room graph contracts/kernel; no legacy graph, Runtime, Catalog, ingress, bootstrap, rebuild or SQL workflow implementation read or reused | clean-room in-place authority/execution/lifecycle replacement and failure-first PG evidence (no A/B/C implementation reuse) | One canonical `OperatorGraph` and ordered one/two-source membership replace the flat operator set; one publication/slot/generation, graph transaction, `graph_continuation`, generic `graph_node_state`/graph results, one exported-snapshot `graph_bootstrap` with subordinate member checkpoints, and graph-wide rebuild serve single and multi-input graphs. Every source durably binds its exact default-PK or explicit replica-identity index; Compiler adds ComputedProject and filtered grouped pipelines; Runtime persists graph result contracts without node-kind dispatch. Failure-first fixes cover canonical keyed `value_payload`, all 22 rebuild-prepare contracts, trigger `COALESCE`, and old-invalidated rebuild admission versus post-prepare drift rejection. No adapter, alias, dual write or persisted DeltaBatch exists | `cargo test -p shiba-operator -p shiba-compiler`; Catalog/Runtime/workspace static tests; PG17.10/PG18.4 `scripts/test-m14-graph-runtime.sh`: singleton and cross-schema JOIN, one transaction changing both relations, complete keyed SQL rows, right fan-out/retract, both key changes, injected sink rollback/retry, exact replay, default/explicit/missing identity, exact PK replacement invalidation | M14.7 subsequently closed full bootstrap/rebuild/least-privilege release and performance evidence; SQL parser, outer/three-table join, window/DISTINCT/Min/Max/Avg, plugins and scheduler remain excluded |
| M14.7 full graph release closure | M14.1--M14.6 clean-room contracts, implementation and directed evidence plus the unchanged M1--M13 release gates; no legacy performance harness, graph implementation or authority read or reused | clean-room exact-enrollment release, recovery and performance evidence (no A/B/C implementation reuse) | One release runner re-proves the single/multi-source graph Runtime, ACK, one-snapshot bootstrap, graph-wide rebuild, recovery, DDL, concurrency and least privilege. Failure-first boundaries preserve zero-column singleton CountRows without identity only for the empty layout, proven composite single-source identity, exactly one bigint PK/UK JOIN-right identity, and replay-before-current-eligibility only after graph/generation lock. No threshold, workload or assertion is relaxed | Commit `206f085`, `scripts/release-matrix.sh`: 51 unique scripts, PostgreSQL 17.10 + 18.4, 102 successful invocations. Five-run M13 same-scene Apply medians 771.019625/821.920250 ms versus 782.302750/787.157125 ms baselines (-1.44%/+4.42%), below 899.648163/905.230694 ms ceilings. M12 totals 7.588033917/7.703419083 s, RSS +6224/+6272 KiB, retained WAL 252905752/252938872 bytes below 268435456 | M14 completes its declared Operator Graph scope, not V2. The later lifecycle evidence closure addresses the reviewer gap and raises final enrollment to 52/104. SQL frontend, outer/three-table join, windows, DISTINCT/Min/Max/Avg, broader types/results, plugins, scheduler, cross-host failover and production soak remain unproved |
| M14 lifecycle evidence closure | M10 ACK, M11 exported-snapshot, M12 rebuild and M14 graph contracts plus reviewer-identified evidence gap; no legacy lifecycle, receiver, bootstrap, rebuild or test implementation read or reused | clean-room real-PG vertical evidence only; production behavior unchanged (no A/B/C implementation reuse) | Exercise one two-source cross-schema Join graph through registration, one exported snapshot scanning both members, concurrent two-source WAL catch-up, atomic activation, governed live Apply/explicit ACK, deterministic Apply-before-ACK session loss, `AlreadyApplied` replay/ACK, same-binding non-pristine whole-graph generation-1-to-2 rebuild and post-cutover live ACK. Mechanical responsibility splits leave all production files below 300 lines without API, authority or transaction changes | PG17.10/PG18.4 `scripts/test-m14-join-lifecycle.sh`: full keyed SQL oracle at every phase, exact graph continuation cardinality/generation, confirmed-flush before/after ACK, generation-2 slot and post-cutover live result. Exact final release enrollment passed 52 scripts/104 invocations | M14 completes its declared graph lifecycle evidence, not V2. SQL frontend, broader joins/operators/types, failover and soak remain unproved |
| M15.1 SQL frontend and QuerySpec contract | M14 canonical OperatorGraph, registration transaction and graph-wide rebuild authority plus current `sqlparser` 0.62.0 primary documentation; no legacy parser, SQL workflow, declaration or compiler implementation read or reused | clean-room contract/ADR and failure-first static evidence only (no A/B/C implementation reuse) | Freeze bounded PostgreSQL-style SELECT input, canonical generic QuerySpecV1 as sole durable declaration, exact ObjectAddress binding, OperatorGraph as sole execution authority, stable byte-span errors, DDL-safe registration, target-descriptor rebuild rebind, parser dependency boundary and explicit rejection matrix. GraphOutputSpecV1 must be deleted at M15.2 rather than adapted | `scripts/check-m15-contract.sh`, PG17.10/PG18.4 L0, Markdown/diff/static authority scan; official sqlparser 0.62.0 docs prove PostgreSqlDialect, recursion limit, Spanned and TokenWithSpan capabilities but not Shiba semantics | Parser, QuerySpec production cutover, binder, SQL-to-graph PG differential and lifecycle evidence remain M15.2--M15.7; SQL dialect approximation and frontend heap measurement remain to be proved |
| M15.2 generic QuerySpec production cutover | M15.1 clean-room QuerySpec contract and M14 canonical OperatorGraph/registration/rebuild interfaces; no legacy declaration/compiler/SQL workflow read or reused | clean-room implementation and release evidence (no A/B/C implementation reuse) | Replace GraphSpecV1/GraphOutputSpecV1 recipes and decoder with strict generic QuerySpecV1 nodes/results; compiler version 2 stores canonical QuerySpec; registration and rebuild share `compile_query`; OperatorGraph remains sole Runtime authority. No old decoder, adapter, dual format, parser AST or SQL text enters durable state | `cargo test -p shiba-compiler`: 10 tests covering canonical digest/topology, global expression/boolean/depth/name/payload bounds, exact catalog type coordinates, generic single/two-source M14 shapes and corrupt/old rejection. `scripts/release-matrix.sh` on PG17.10/PG18.4: 52 unique scripts, 104 invocations, all green, including M9 registration, M11 bootstrap/recovery, M12 million-row rebuild and M14 Join lifecycle. M12 totals 8.144607917/8.163367334 s, RSS +6256/+6528 KiB, retained WAL 252905752/252938872 bytes below 256 MiB | M15.2 is green but M15 is not complete. SQL parser/unbound lowering, stable frontend diagnostics, SQL differential, frontend DDL races/least privilege and frontend performance remain later M15 work |
| M15.3 bounded SQL parser and UnboundQuery | M15.1 clean-room SQL frontend contract, `sqlparser` 0.62.0 public parser/token/span APIs and M15.2 canonical QuerySpec boundary; no legacy parser, SQL workflow, fixture or implementation read or reused | clean-room pure parser implementation and evidence (no A/B/C implementation reuse) | Pin `sqlparser` exactly 0.62.0 with default features disabled and only `std`; lower the explicit PostgreSQL-style SELECT allowlist into ephemeral `UnboundQuery`; own stable half-open byte spans, closed snake_case errors and hard byte/token/AST/expression limits; iteratively validate public AST before canonical payload/digest. Failure-first testing found and removed the 4,096-token left-deep AST recursive-drop stack risk by rejecting above the conservative structural ceiling before parser AST construction. No SQL/parser type or dependency enters Runtime, Ingress, Operator, Catalog authority, transaction, continuation or ACK | 22 pure frontend tests plus doc-tests; fmt/check/clippy `-D warnings`; `scripts/check-m15-contract.sh`; `scripts/check-m15-parser.sh`; dependency tree contains only `sqlparser` `std` plus pure Rust support and no C-toolchain/PostgreSQL runtime. Accepted/rejected, exact UTF-8 spans, bounds, canonical golden/metamorphic, malformed/no-panic and public-AST fail-closed cases green | Binder/type checker, SourceId/ObjectAddress resolution, QuerySpec lowering, registration, PG17.10/PG18.4 SQL differential, DDL race/lifecycle and 10,000-query performance remain unproved. M15.3 is not M15 completion |
| M15.4 pure Binder and first production SQL vertical | M15.1--M15.3 clean-room SQL/QuerySpec contracts and M10--M14 clean-room registration, receiver, bootstrap, ACK and rebuild APIs; no legacy parser, binder, SQL workflow, registration or lifecycle implementation/test was read or reused | clean-room pure binding, control-plane registration and directed real-PG evidence (no A/B/C implementation reuse) | Pure Binder maps one exact `ResolvedSource` into generic QuerySpec for the admitted keyed projection/filter/checked-compute shape. Separate `shiba-sql-registration` parses before a short transaction, resolves one schema-qualified relation to a unique registered SourceId, locks/revalidates exact binding/ObjectAddresses/invalidation, invokes the Binder, and delegates all durable writes to Runtime's transaction-local sole registration writer. Failure-first cases inject result registration failure and replace the payload column while registration is observably blocked; both leave no partial graph authority. Raw SQL/parser AST remain ephemeral; no adapter, second writer, authority, continuation or ACK path is introduced | Pure Binder/control-plane tests plus `scripts/check-m15-registration.sh`; PG17.10/PG18.4 directed `scripts/test-m15-sql-vertical.sh` proves quoted cross-schema registration/rollback, DDL-first `ddl_drift`, snapshot plus concurrent I/U/D catch-up, building visibility, live Apply/ACK, `AlreadyApplied` replay, complete key/value SQL oracle, changed relation/column/index/publication ObjectAddress rebuild and post-cutover live ACK. The script is enrolled as gate 53; the complete 53-script/106-invocation matrix is not claimed as M15.4 evidence | M15.5 aggregate SQL, M15.6 two-table Join SQL, M15.7 least-privilege registration, frozen parser/registration performance and final full matrix remain unproved. M15.4 is not M15 completion |
| V1/V2 巨石、`sql/00_catalog.sql`、old authorities | `/Users/zzhang/Documents/Shiba/sql/00_catalog.sql` and monolith modules | C | Reference only; never migrate | L0 gate; manual review | None: explicitly prohibited |
| publications/change logs; runtime/operator SQL; registration mixes | `/Users/zzhang/Documents/Shiba/sql/10_runtime.sql`, `11_ingress.sql`, `12_effect_stream.sql`, `30_registration.sql` | C | Reference only; never migrate | L0 gate; manual review | None: explicitly prohibited |
| fallback/alias/adapter/dual-write and locally tested unowned code | Legacy paths and any candidate lacking recovery/transaction proof | C | Never migrate | L0 gate plus ownership review | None: explicitly prohibited |

Review rule: the main agent audits every row before a fixture or B-class rewrite is
accepted. An absolute legacy path is provenance, never an import path.
