[package]
name = "shiba-operator"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Database-independent operator contracts for Shiba V2"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
shiba-protocol = { path = "../shiba-protocol" }

[dev-dependencies]
serde_json = "1.0"

[lints]
workspace = true
